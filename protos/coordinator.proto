syntax = "proto3";

package coordinator;

service Coordinator {
  rpc requestTask(Worker) returns (WorkerTask) {}
}

message Worker {
  string id = 1;
}

message WorkerTask {
  int32 type = 1;
  string file_in = 2;
}
